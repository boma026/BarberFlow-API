version: "2"
sql:
  - engine: "postgresql"
    queries: "db/query/queries.sql"
    schema: "db/schema/schema.sql"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true